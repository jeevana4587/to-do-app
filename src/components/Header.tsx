export function Header() {
  return (
    <header className="app-header">
      <p className="eyebrow">Daily focus</p>
      <h1>FocusList</h1>
      <p className="lede">Capture the work that matters, then finish it one task at a time.</p>
    </header>
  );
}
